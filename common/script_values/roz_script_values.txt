roz_mega_wanxiangtianyin_trigger_values = {
	#roz_tempvar备用
	base = 1
	max = 10
	modifier = {
		add = value:scripted_modifier_mult|MODIFIER|leader_skill_levels|MODIFIER|leader_initial_skill|
		exists = capital_scope
		capital_scope = {
			has_building = building_llk_paradox_origin
		}
	}
	modifier = {
		add = 1
		owner = {
			check_variable = {
				which = roz_leader_aft_suigintou_reduced_score
				value > 40
			}
		}
	}
	modifier = {
		add = 1
		owner = {
			check_variable = {
				which = roz_leader_aft_suigintou_reduced_score
				value > 50
			}
		}
	}
	modifier = {
		add = 1
		owner = {
			check_variable = {
				which = roz_leader_aft_suigintou_reduced_score
				value > 60
			}
		}
	}
	modifier = {
		add = 1
		owner = {
			check_variable = {
				which = roz_leader_aft_suigintou_reduced_score
				value > 70
			}
		}
	}
	modifier = {
		add = 1
		owner = {
			check_variable = {
				which = roz_leader_aft_suigintou_reduced_score
				value > 80
			}
		}
	}
	floor = yes
}

ge_modifier_factor_tag = {
	base = 0
	add = 1
	add = modifier:ship_weapon_damage
	modifier = {
		add = modifier:weapon_type_explosive_weapon_damage_mult
		always = $EXPLOSIVE$
	}
	add = modifier:weapon_type_ge_void_energy_weapon_damage_mult
	mult = modifier:ship_hull_damage_mult
	min = 1
}

ge_damage_ship_max_hp_limit = {
	base = 0
	add = trigger:has_hp
	subtract = 2.0
	floor = yes
	min = 0.0
}

ge_wtorpedo_damage_modifier_amount = {
	add = 17900
	mult = value:ge_modifier_factor_tag|EXPLOSIVE|yes|
    mult = 2.5
	max = value:ge_damage_ship_max_hp_limit
}

ge_vn_torpedo_damage_modifier_amount = {
	add = 18500
	mult = value:ge_modifier_factor_tag|EXPLOSIVE|no|
	mult = 4.0
	max = value:ge_damage_ship_max_hp_limit
}

ge_sgt_torpedo_damage_modifier_amount = {
	add = 800
	mult = value:ge_modifier_factor_tag|EXPLOSIVE|yes|
	mult = 1.25
	max = value:ge_damage_ship_max_hp_limit
}

ge_gravity_burst_damage_ship_count = {
	base = 0
	add = trigger:has_max_hp
	mult = 0.05
	max = value:ge_damage_ship_max_hp_limit
}

ge_roz_unholy_invasion_difficulty_fleet_var = {
	base = 0
	min = 0
	add = 0.5
	mult = event_target:global_event_country.roz_unholy_invasion_difficulty
}

#恐惧
immune_chance_terrify = {
	base = 1
	add = modifier:immune_chance_terrify
	min = 0
}
ship_terrify_chance = {
	base = 0
	modifier = { add = 25 ship_size_multiplier < 2 }
	modifier = { add = 20 ship_size_multiplier >= 2 ship_size_multiplier < 4 }
	modifier = { add = 10 ship_size_multiplier >= 4 ship_size_multiplier < 8 }
	modifier = { add = 3 ship_size_multiplier >= 8 ship_size_multiplier < 16 }
	modifier = { add = 1 ship_size_multiplier >= 16 }
	modifier = {
		factor = 0
		OR = {
			has_ship_flag = immune_to_terrify
			check_modifier_value = { modifier = immune_to_terrify value > 0 }
		}
	}
	mult = value:immune_chance_terrify
}
terrify_duration_factor = {
	base = 1
	add = modifier:terrify_duration_mult
	min = 0
}
ship_terrify_duration = {
	base = 0
	modifier = { add = 10 ship_size_multiplier < 2 }
	modifier = { add = 8 ship_size_multiplier >= 2 ship_size_multiplier < 4 }
	modifier = { add = 6 ship_size_multiplier >= 4 ship_size_multiplier < 8 }
	modifier = { add = 5 ship_size_multiplier >= 8 }
	add = modifier:terrify_duration_add
	mult = value:terrify_duration_factor
}

#codes from kuat
roz_pop_free_house = {
	base = 0
	add = trigger:free_housing
	mult = 0.01
	min = 0
}

roz_attendant_growth = {
	base = 1
	complex_trigger_modifier = {
		trigger = num_districts
		parameters = { type = any }
		mode = add
	}
	complex_trigger_modifier = {
		trigger = count_owned_pop_group
		potential = { is_scope_type = planet }
		trigger_scope = this
		mode = divide
	}
	modifier = {
		has_modifier = planet_population_control
		set = 0
	}
}

roz_planet_pop_assembly_average_int = {
	base = 0
	add = value:roz_attendant_growth
	max = value:roz_pop_free_house
}
#

